apiVersion: v1
kind: Pod
metadata:
  name: worker-app-pod
  labels:
    # labels allows services to search PODs.
    name: worker-app-pod
    app: demo-voting-app
spec:
  containers:
    - name: worker-app
      image: dockersamples/examplevotingapp_worker


